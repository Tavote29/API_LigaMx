name: CI/CD Liga MX API

on:
  # Fase 1 (CI): Se dispara al hacer un Pull Request hacia main
  pull_request:
    branches: [ "main" ]
  # Fase 2 (CD): Se dispara cuando el código se fusiona en main
  push:
    branches: [ "main" ]

jobs:

  # 1 (Pruebas y Compilación)
  build-and-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./liga-mx # Apunta a tu subcarpeta de Spring Boot
        
    steps:
      - name: Descargar el código del repositorio
        uses: actions/checkout@v3

      - name: Configurar Java 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Compilar y Ejecutar Pruebas
        run: mvn clean test  


  # 2:(Docker y Producción)
  deploy:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: build-and-test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./liga-mx 
        
    steps:
      - name: Descargar el código
        uses: actions/checkout@v3

     
      - name: Configurar Java 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Empaquetar la aplicación (.jar)
       
        run: mvn clean package -DskipTests
      

      - name: Iniciar sesión en Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Construir y subir imagen Docker
        run: |
          docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/api-ligamx:latest .
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/api-ligamx:latest

      